The following folder contains

    The source code for the reqistration of the devices
    The docker file of the image from the source code and the requirements.txt
        In order to create the image
            Create the folder in your local machine
            To build the image ,execute the following commands $ cd foldername (where are the files ) $ sudo docker build -t foldername/dockerhubrepo:tag .

The images are located:

- DockerHub: panoschor/bank:

#In order to pull the image 
$docker pull panoschor/bank:statistics-1.0.0
